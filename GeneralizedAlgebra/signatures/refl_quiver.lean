import GeneralizedAlgebra.signatures.quiver

def 𝔯𝔔𝔲𝔦𝔳 : GAT := --⟨
⦃
    V : U,
    E : V ⇒ V ⇒ U,
    r : (v : V) ⇒ E v v
⦄
-- ,
-- λ P => _
-- ⟩

-- def foo := ⦃
--     V : U,
--     E : V ⇒ U,
--     r : (v : V) ⇒ E v
-- ⦄

-- #eval foo.telescopes
